{
    "forbidden": [
        {
            "name": "group-two-to-pathnot",
            "comment": "not to peer folders directly under source (except self and 'shared')",
            "from": {
                "path": "^(src)/([^/]+)/.+"
            },
            "to": {
                "path": "^src/[^/]+/.+",
                "pathNot": "^src/($2|shared)/.+"
            }
        }
    ]
}
