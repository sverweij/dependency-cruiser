{
    "compileOnSave": true, // base only
    "exclude": [
        "base only"
    ],
    "include": [
        "from base, but overridden by extends", // overridden by extends
        "from base, but overridden by extends as well" // overridden by extends
    ],
    "files": [
        "./dummysrc.ts"
    ]
}