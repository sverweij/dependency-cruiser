version: "2"
checks:
  method-lines:
    config:
      threshold: 30
plugins:
  eslint:
    enabled: true
    channel: "eslint-4"
    config: 
      config: .eslintrc.json
    checks:
      compat/compat:
        enabled: false  
exclude_patterns:
  - "docs/"
  - "docs/"
  - "test/"
  - "**/node_modules/"
  - "**/*.d.ts"
  - "src/report/**/*.template.js"